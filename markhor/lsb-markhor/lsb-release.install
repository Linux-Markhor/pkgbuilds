post_install() {
  echo "Markhor Linux release" >> /etc/markhorlinux-release
}

post_upgrade() {
  sed -e "/Markhor Linux release/d" -i /etc/markhorlinux-release
  echo "Markhor Linux release" >> /etc/markhorlinux-release
}

pre_remove() {
  sed -e "/Markhor Linux release/d" -i /etc/markhorlinux-release
}
